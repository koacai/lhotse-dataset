[project]
name = "lhotse-dataset"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Kohei ASAI", email = "ko2001he@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.4.1",
    "gdown>=5.2.0",
    "gitpython>=3.1.45",
    "hydra-core>=1.3.2",
    "lhotse>=1.32.0",
    "librosa>=0.11.0",
    "omegaconf>=2.3.0",
    "pydantic>=2.12.4",
    "soundfile>=0.13.1",
    "torchcodec>=0.8.1",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lhotse_dataset"]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
    "ruff>=0.14.6",
]

[tool.ruff]
exclude = [".venv"]
indent-width = 4
fix = true

[tool.ruff.lint]
select = ["E", "F", "I"]
fixable = ["ALL"]

[tool.ruff.format]
docstring-code-format = true

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.pytest.ini_options]
addopts = "-vv --tb=short -s"
